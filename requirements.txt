pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
pymc>=5.0               # Important: PyMC v5+ (uses aesara → pytensor)
arviz>=0.15             # For inference diagnostics & plots
jupyterlab
ipywidgets
plotly>=5.0             # Great for interactive plots & dashboard
dash>=2.0               # Recommended for quick dashboard (Python-only)
dash-bootstrap-components
tqdm